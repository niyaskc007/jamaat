# Empty so axios issues same-origin relative URLs. Vite's dev server (port 5173) proxies
# /api/* to the API per vite.config.ts. The proxy target there - http://localhost:5174 -
# is the dev API port; change if you run the API on a different port locally.
VITE_API_BASE_URL=
VITE_DEFAULT_LANGUAGE=en
